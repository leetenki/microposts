<%= form_for(current_user.following_relationships.find_by(:followed_id => user.id), :html => {:method => :delete}, :remote => true) do |f| %>
	<%= f.submit "Unfollow", :class => "btn" %>
  <div><%= hidden_field_tag :page_owner_id, page_owner.id %></div>  
<% end %>
